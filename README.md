# sol.pizza — the main site

The public site for Sol, an Italian-American restaurant in Tây Hồ, Hanoi, in the
"Printed menu" design: cream paper, centred type, red rules. Static HTML, no
framework, generated by `build/gen.py` and served by the Cloudflare Worker
`sol-pizza` (see `README-DEPLOY.md`).

| Route | Page |
| --- | --- |
| `/` | Home: the hero and the facts row |
| `/about/` | About tab: the restaurant introduction and our story (the old `/story/` redirects here) |
| `/menu/` | Menu tab: Food |
| `/menu/wine/` | Menu: Wine (the old `/wine/` redirects here) |
| `/menu/bar/` | Menu: Bar |
| `/booking/` | Booking tab: the booking box at `/booking/#book`, hours, address, contact (the old `/visit/` redirects here) |
| `/jobs/` | Work with us: the opening-team cards, plus the role pages under `/jobs/` |
| `/jobs/<slug>/` | The six opening-team roles: `host`, `server`, `bartender`, `pizzaiolo`, `line-cook`, `kitchen-porter` |
| `/privacy/` | Applicant privacy notice |

The header has three tabs, About · Menu · Booking (`NAV_TABS` in `build/gen.py`),
plus the EN · VI switch; Menu keeps its Food / Wine / Bar sub-tabs. The mobile nav
sheet and the footer list the same three.

---

## Build and deploy

```
npm ci                 # once: fonts, the minifiers and wrangler
npm run build          # writes ./dist
npx wrangler dev       # check it locally (applies _redirects, unlike a plain static server)
npx wrangler deploy    # see README-DEPLOY.md
```

---

## Editing

Everything is generated from `build/gen.py`; edit there and rebuild. Anything
you hand-edit in `dist/` is overwritten by the next build.

### Words

Every line of copy appears twice, English then Vietnamese:

```python
t('Book a table', 'Đặt bàn')
```

Edit both, or the two languages drift apart. Vietnamese changes for review are
logged in `VI-CHANGES.md`.

### The menu

Food, wine and bar live in `build/menu-data.json`. Each English field has a
`_vi` twin below it. Dish, wine and drink names stay as written in both
languages; only descriptions, section names and place names are translated.
There are no prices yet: when they're set, add them to the data and to the
item template in `build_food()` / `build_wine()` / `build_bar()`.

The count lines on the wine page ("3 wines, 2 by the glass") and the Wine tab
are generated from the data. The wine lead ("Thirty wines…") and the home and
wine meta descriptions ("26 wines by the glass") spell the counts out in words,
so the build stops if the list no longer has 30 wines, 26 by the glass, and
tells you what to update.

### Booking

Online booking waits for ResDiary. Once the widget is set up (ResDiary →
Promote → Widget configurator → Embed Code), paste its URL into
`RESDIARY_URL` near the top of `build/gen.py` and rebuild: the Booking page's
booking box then shows the widget and says "Book online below, or call or
email us."

### Jobs

The opening team is six roles, each with a card on `/jobs/` (grouped Front of
house / Kitchen) and a page at `/jobs/<slug>/` in both languages:

- `build/jobs-data.json`: the round (posted, closing instant, start date,
  service charge estimate) and each role's slug, section, openings, base salary,
  card line and apply question, English and Vietnamese. The closing instant is
  in here once; everything else that shows a date or a total is built from it.
- `build/jobs/<slug>.md`: the job description word for word, the English block
  then the Vietnamese one (`# ` title, the eyebrow line, the facts table, the
  bold standfirst, then `##` / `###` headings, paragraphs and `- ` lists). The
  build stops if a description's base salary, service charge, total or openings
  no longer match the data, because the ads quote the same numbers.

Apply buttons open an email to jobs@sol.pizza with the subject (`Pizzaiolo — `)
and a body in the page's language; switching EN / VI switches the email too.
After the closing instant a small script in the page head shows "Applications
for this round closed…" on the cards and pages and hides every Apply button. To
check it, set `closes` to yesterday, build, look, and set it back (the build
notes that the descriptions still mention the old date; that's expected).
The share card for each role is in Vietnamese (the ads bring Vietnamese
speakers) and each page carries a `JobPosting` for search.

Filled roles are listed under "Recently filled" and link to their pages, which
are the original pages in `src/jobs/` re-wrapped in the site design with their
copy untouched: a "This role has been filled" note at the top, no apply button,
`noindex`. Those three are in English only; Vietnamese readers see a one-line
note.

---

## How the language switch works

EN / VI buttons in the header (and in the mobile menu). Both languages are in
the HTML; `build/design/site.css` hides the one `<html lang>` doesn't ask for.
The choice is remembered in the browser (`localStorage` key `sol-lang`) and
applied by a small script in `<head>` before the page paints, so there is no
flash of the other language. A link ending `?lang=vi` opens a page in
Vietnamese. The page title, meta description and every `aria-label` switch too.

Vietnamese pages always use the free fonts (Philosopher, EB Garamond): the
brand fonts don't include the Vietnamese letters.

---

## Fonts

Two typefaces and seven sizes, set in `build/design/tokens.css`: a display face
for headings and a serif for everything else, plus one hand-written phrase per
section. Small labels are the serif in capitals.

- **EB Garamond** (everything that isn't a heading, in both languages) is one
  of the free fonts below. It replaced Gryphius MVB on 24 Sep 2026, which read
  badly on screen and has no Vietnamese letters; nothing uses Gryphius now.
- **BN Arora** (headings, English pages) is self-hosted: it's licensed for web
  use, and the file travels in `build/assets.b64.json`. It has no Vietnamese
  letters, so Vietnamese headings use Philosopher. `build/README.md` has the
  details.
- **Plunct** (the one hand-written phrase per section, English pages; upright)
  comes from Sol's Adobe Fonts web project, kit `umo0non`, linked on every page
  without blocking the first paint. It can't be self-hosted, so never download
  or commit its fonts. Vietnamese pages set the phrase in EB Garamond italic.
  The kit also holds Gryphius MVB and Gryphius MVB Small Caps, which the site
  doesn't use; removing them from the web project makes the kit a little
  lighter. Keep the Creative Cloud plan behind the web project active. In Adobe
  Fonts, set the web project's font display to **swap** (it is "auto" today,
  which can hide the phrase for up to 3 seconds while Plunct loads).
- **Free fonts**, always loaded and Vietnamese-complete: Philosopher and EB
  Garamond, from the `@fontsource` npm packages. The build
  copies only the Latin, Latin Extended and Vietnamese files into
  `dist/assets/fonts/`.

---

## What's in the box

```
build/gen.py            the generator: every page, the shell, redirects, sitemap
build/menu-data.json    food, wine, bar
build/jobs-data.json    the opening-team round and its six roles
build/jobs/             the six job descriptions, English and Vietnamese (Markdown)
build/design/           tokens.css, printed-menu.css (the design), site.css (repo
                        additions), printed-menu.js (menu tabs, mobile nav, EN / VI)
build/assets/           logo, illustrations, sun pattern (SVG)
build/og/               share cards: og-*.html sources, rendered by og.js
build/assets.b64.json   the share-card PNGs and BN Arora, as text (see build/pack_assets.py)
src/jobs/ src/privacy/  role pages and the privacy notice, re-wrapped at build time
src/_redirects          copied to dist/; Cloudflare applies it
src/favicon.svg src/pixel.js
```

The repo is text-only: fonts come from npm or Adobe Fonts, and the share-card
PNGs and BN Arora travel inside `build/assets.b64.json`. After re-rendering the share cards
(`node og.js`), run `npm run pack-assets`.

### Colours

From `Sol_Brand Standards_250619.pdf`, defined in `build/design/tokens.css`:

```
--sol-red  #a72024   sun, wine, tomatoes
--wood     #4e2428   body text
--sun      #f2cc73   button text
--cream    #f9e9d3   the paper
--basil    #506d55
```

---

## Analytics

GA4 (`G-SLBWDVH550`) and the Meta pixel (`856214124247013`) are on every page.
`pixel.js` adds the careers events (ViewContent on `/jobs/`, Lead on a click to
email jobs@sol.pizza). The applicant privacy notice describes both tools.
