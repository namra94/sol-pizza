{
  "name": "sol-pizza",
  "private": true,
  "description": "sol.pizza — static site, built by build/gen.py into dist/, served by the sol-pizza Cloudflare Worker",
  "scripts": {
    "build": "python3 build/gen.py",
    "deploy": "wrangler deploy",
    "pack-assets": "python3 build/pack_assets.py"
  },
  "devDependencies": {
    "@fontsource/eb-garamond": "^5.2.0",
    "@fontsource/philosopher": "^5.3.0",
    "csso": "^5.0.5",
    "terser": "^5.31.0",
    "wrangler": "^4.102.0"
  }
}
